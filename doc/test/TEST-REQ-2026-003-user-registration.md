# 用户自助注册测试文档

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 测试编号 | TEST-REQ-2026-003 |
| 关联需求 | [REQ-2026-003 用户自助注册](../requirements/REQ-2026-003-user-registration.md) |
| 关联详细设计 | [DESIGN-REQ-2026-003 用户自助注册](../design/DESIGN-REQ-2026-003-user-registration.md) |
| 关联数据库设计 | [DB-REQ-2026-003 用户自助注册](../database/DB-REQ-2026-003-user-registration.md) |
| 文档版本 | 0.4 |
| 文档状态 | 待执行 |
| 测试负责人 | 待指定 |
| 测试日期 | 待执行 |

## 2. 测试范围与依据

- 测试目标：验证 Saber、gateway、blade-auth、blade-user-api、blade-system、Redis 和 MySQL 组成的用户自助注册完整链路。
- 范围内：注册开关、默认/显式租户、账号密码校验、图形验证码、注册限流、普通角色、无部门岗位、事务回滚、账号唯一性、注册回跳、登录状态、权限边界和 system 启动依赖检查。
- 范围外：邮件/短信验证、租户创建、注册审批、找回密码、实名/多因素认证和新的 C 端业务页面。
- 验收依据：需求 `AC-001` 至 `AC-034`、`BR-001` 至 `BR-012` 及详细设计第 6 至 13 节。

## 3. 测试环境与准备

| 项目 | 内容 |
| --- | --- |
| 后端版本/提交 | 待指定；需包含 API、auth、system 和 gateway 同版本变更 |
| JDK / Maven | JDK 21 / Maven 3.9.6；模块打包已通过 |
| MySQL | 待指定隔离测试库，不记录连接凭据 |
| Nacos / Redis | 待指定；测试环境开启注册，验证码和限流使用测试 Redis |
| 目标服务 | Saber、blade-gateway、blade-auth、blade-system |
| 测试身份 | 未登录用户、普通用户、管理员、停用/删除用户 |
| 测试数据 | 唯一前缀 `reg20260914_`，只清理本轮创建且归属明确的数据 |

准备要求：默认租户 `000000` 和另一测试租户必须存在、启用、未删除，并各自配置唯一有效的 `role_alias=user` 角色。测试前检查普通角色未绑定用户、角色、租户、菜单、部门和岗位等管理权限。

## 4. 测试用例

### TC-001 注册开关与公开配置

- 优先级：P0
- 关联需求：REQ-001 / AC-001、AC-002、AC-003
- 测试层级：Controller / Gateway / Saber
- 目标模块与入口：`GET /blade-auth/register/config`、`POST /blade-auth/register`、Saber `/login`
- 依赖：Nacos 配置动态生效。
- 前置条件：分别准备开启和关闭注册的配置状态。
- 测试数据：无敏感数据。
- 步骤：
  1. 开启注册并打开登录页，查询注册配置。
  2. 关闭注册并刷新页面，再直接提交结构合法的注册请求。
  3. 使用已有账号执行原登录流程。
- 预期结果：
  - HTTP 状态与业务码：配置接口返回 200；关闭时注册返回 `48301`。
  - `R<T>` 响应结构：仅包含开关、默认租户和长度规则，不包含角色、数据库或 Redis 信息。
  - 数据、事务和缓存结果：关闭时不创建用户。
  - 租户、权限或降级结果：页面无可提交注册入口，原登录功能正常。
- 证据：待记录配置响应、页面截图和用户表查询结果。
- 清理：恢复测试环境约定的注册开关状态。
- 实际结果：待执行
- 结果：未执行

### TC-002 默认租户成功注册与登录回跳

- 优先级：P0
- 关联需求：REQ-002、REQ-003、REQ-005、REQ-006 / AC-004、AC-008、AC-009、AC-017、AC-022、AC-023、AC-024
- 测试层级：HTTP / Service / MySQL / Saber / 集成
- 目标模块与入口：Saber 注册表单、`POST /blade-auth/register`
- 依赖：默认租户和普通角色有效，Redis 可用。
- 前置条件：注册开启，取得新的图形验证码。
- 测试数据：租户留空，账号 `reg20260914_default01`，昵称留空，密码满足规则且不写入证据。
- 步骤：
  1. 提交合法注册表单。
  2. 检查浏览器 Network 中请求 `tenantId` 的 JSON 类型、注册响应、用户表字段和角色归属。
  3. 检查页面返回登录模式并使用新账号登录。
- 预期结果：
  - HTTP 状态与业务码：注册返回 200/成功。
  - `R<T>` 响应结构：请求和响应均使用字符串 `tenantId="000000"`，不得变为数值 `0`；只返回账号和 `nextAction=LOGIN`，不返回令牌。
  - 数据、事务和缓存结果：创建一条启用、未删除用户；昵称等于账号，密码为摘要，部门/岗位为 `-1`。
  - 租户、权限或降级结果：角色为默认租户唯一 `user` 角色；注册后仍未登录。
- 证据：待记录响应、用户表查询、登录页状态和登录结果。
- 清理：按租户和账号精确清理该测试用户及对应测试缓存。
- 实际结果：待执行
- 结果：未执行

### TC-003 显式租户、无效租户与租户隔离

- 优先级：P0
- 关联需求：REQ-002、REQ-007 / AC-005、AC-006、AC-007、AC-028
- 测试层级：HTTP / Service / MySQL / 集成
- 目标模块与入口：`POST /blade-auth/register`
- 依赖：两个有效测试租户和一个停用/删除租户。
- 前置条件：各有效租户有唯一普通角色。
- 测试数据：同一账号 `reg20260914_shared01` 分别注册到两个有效租户；另使用不存在、停用和删除租户。
- 步骤：
  1. 在两个有效租户分别注册同一账号。
  2. 向不存在、停用和删除租户提交合法注册请求。
  3. 检查用户和角色归属。
- 预期结果：
  - HTTP 状态与业务码：有效租户均成功；无效租户返回 `48305`。
  - `R<T>` 响应结构：只返回当前目标租户和账号。
  - 数据、事务和缓存结果：无效租户不创建或修改租户、角色、用户。
  - 租户、权限或降级结果：两个同名用户归属、角色和后续访问相互隔离。
- 证据：待记录各响应和按租户查询的用户数据。
- 清理：精确清理两个有效租户下本轮新增用户，恢复临时租户状态。
- 实际结果：待执行
- 结果：未执行

### TC-004 账号、密码、昵称和受控字段校验

- 优先级：P0
- 关联需求：REQ-003、REQ-005 / AC-010、AC-011、AC-012、AC-018
- 测试层级：Saber / Controller / Service
- 目标模块与入口：注册表单、`POST /blade-auth/register`
- 依赖：每次服务端请求使用新的验证码。
- 前置条件：注册开启，目标租户有效。
- 测试数据：账号为空/过短/过长/非法字符/仅空白；密码越界、缺字母、缺数字、与账号相同；确认密码不一致；昵称超过 32 位；附加伪造角色、部门、岗位、状态和删除字段。
- 步骤：逐项提交并检查前端校验、服务端结果、日志和用户表。
- 预期结果：
  - HTTP 状态与业务码：参数绑定失败或返回 `48302`。
  - `R<T>` 响应结构：不回显密码、确认密码、SM2 密文、摘要或验证码。
  - 数据、事务和缓存结果：非法请求不创建用户。
  - 租户、权限或降级结果：客户端无法决定角色、部门、岗位、状态和删除标识。
- 证据：待记录非敏感请求摘要、响应码和数据库计数。
- 清理：无用户数据；清理本用例测试验证码和限流键。
- 实际结果：待执行
- 结果：未执行

### TC-005 图形验证码一次性消费

- 优先级：P0
- 关联需求：REQ-004 / AC-013、AC-014、AC-016
- 测试层级：HTTP / Redis / 集成
- 目标模块与入口：`GET /blade-auth/captcha`、`POST /blade-auth/register`
- 依赖：Redis 可用；邮件和短信服务不配置。
- 前置条件：注册开启。
- 测试数据：正确、错误、过期和已使用验证码。
- 步骤：
  1. 使用有效验证码提交一次。
  2. 重复使用相同验证码，再分别使用错误和过期验证码提交。
  3. 检查 Redis 键和用户表。
- 预期结果：
  - HTTP 状态与业务码：有效验证码通过，其余返回 `48303`。
  - `R<T>` 响应结构：不返回验证码内容或 Redis Key。
  - 数据、事务和缓存结果：验证码校验后不可复用，无效验证码不创建用户。
  - 租户、权限或降级结果：不依赖邮件或短信服务。
- 证据：待记录验证码键消费状态、响应和用户表查询。
- 清理：清理成功创建的测试用户和本用例 Redis 临时键。
- 实际结果：待执行
- 结果：未执行

### TC-006 IP、租户和账号频率限制

- 优先级：P0
- 关联需求：REQ-004 / AC-015
- 测试层级：HTTP / Redis / 集成
- 目标模块与入口：`POST /blade-auth/register`
- 依赖：可临时降低测试环境阈值并观察 TTL。
- 前置条件：每次请求使用新的验证码。
- 测试数据：同一来源、租户和账号的连续请求。
- 步骤：分别超过 IP、租户和账号维度阈值，随后等待窗口到期再提交。
- 预期结果：
  - HTTP 状态与业务码：超过任一阈值返回 `48304`。
  - `R<T>` 响应结构：只返回稳定提示，不暴露内部键和完整阈值配置。
  - 数据、事务和缓存结果：超限请求不创建用户，计数键具有正确 TTL。
  - 租户、权限或降级结果：窗口到期后恢复请求能力。
- 证据：待记录非敏感响应、计数和 TTL。
- 清理：只删除本用例可识别的限流键，恢复原测试阈值。
- 实际结果：待执行
- 结果：未执行

### TC-007 普通角色缺失、重复和跨租户角色

- 优先级：P0
- 关联需求：REQ-005 / AC-017、AC-019、AC-020、AC-021
- 测试层级：Service / Feign / MySQL
- 目标模块与入口：`IUserClient.register`、`IUserService.register`
- 依赖：可控制目标租户角色配置和用户保存异常。
- 前置条件：准备角色正常、缺失、重复和仅其他租户存在 `user` 角色的场景。
- 测试数据：合法注册命令和唯一账号。
- 步骤：
  1. 分别执行四类角色配置下的注册。
  2. 模拟用户保存失败并检查事务结果。
  3. 检查部门树和岗位数据。
- 预期结果：
  - HTTP 状态与业务码：正常配置成功；其他场景返回 `48306` 或 `48309`。
  - `R<T>` 响应结构：不返回角色 ID 和内部异常。
  - 数据、事务和缓存结果：失败时无半成品用户，不新增部门/岗位；成功用户无部门、无岗位。
  - 租户、权限或降级结果：不回退 admin、administrator 或其他租户角色。
- 证据：待记录角色查询、事务后用户计数和组织树结果。
- 清理：恢复临时角色配置，清理成功创建的测试用户。
- 实际结果：待执行
- 结果：未执行

### TC-008 同租户重复、大小写、逻辑删除和并发注册

- 优先级：P0
- 关联需求：REQ-007 / AC-026、AC-027、AC-029、AC-030
- 测试层级：Service / MySQL / 并发集成
- 目标模块与入口：账号预检查、唯一索引、注册接口
- 依赖：数据库已执行本需求升级脚本。
- 前置条件：准备现存账号、大小写变体和逻辑删除账号。
- 测试数据：账号 `reg20260914_case01` 及大小写变体；两个并发请求使用同租户同账号。
- 步骤：
  1. 提交重复、大小写变体和逻辑删除账号注册。
  2. 并发提交两个相同新账号请求。
  3. 查询数据库记录数和异常响应。
- 预期结果：
  - HTTP 状态与业务码：重复场景返回 `48307`；并发最多一个成功。
  - `R<T>` 响应结构：不暴露 SQL、约束名或堆栈。
  - 数据、事务和缓存结果：同租户账号最多一条，逻辑删除账号仍占用。
  - 租户、权限或降级结果：大小写比较不敏感，不影响跨租户同名。
- 证据：待记录并发响应和用户表唯一计数。
- 清理：按测试租户、账号和主键精确清理新增记录，保留既有前置数据。
- 实际结果：待执行
- 结果：未执行

### TC-009 Feign Fallback、远端业务失败和缓存异常

- 优先级：P0
- 关联需求：REQ-005、REQ-006 / AC-020、AC-025
- 测试层级：Feign / Service / 集成
- 目标模块与入口：`IUserClient.register`、Fallback、auth 注册编排
- 依赖：可模拟 system 超时、Fallback、业务失败和缓存清理异常。
- 前置条件：注册请求在 auth 本地校验均通过。
- 测试数据：合法注册请求和唯一账号。
- 步骤：
  1. 模拟 Feign 超时/Fallback 和 system 业务失败。
  2. 模拟用户已保存后的缓存清理异常。
  3. 检查 auth 判断、用户数据和客户端重试。
- 预期结果：
  - HTTP 状态与业务码：不可用返回 `48308`，已知业务失败保持其业务码。
  - `R<T>` 响应结构：null、空数据和 `success=false` 均不被视为成功。
  - 数据、事务和缓存结果：远端失败不产生 auth 本地半成品；用户保存成功时缓存异常不回滚用户。
  - 租户、权限或降级结果：重试不会创建第二条账号。
- 证据：待记录 Feign 响应、用户表和缓存回源结果。
- 清理：清理本用例成功用户和故障模拟配置。
- 实际结果：待执行
- 结果：未执行

### TC-010 注册结果、页面敏感状态与失败重试

- 优先级：P0
- 关联需求：REQ-006 / AC-022、AC-023、AC-024、AC-025
- 测试层级：Saber / HTTP
- 目标模块与入口：Saber 登录/注册模式
- 依赖：注册接口和验证码可用。
- 前置条件：准备一次成功和一次可修正失败。
- 测试数据：唯一账号；密码和验证码不写入证据。
- 步骤：
  1. 完成成功注册并检查登录表单、浏览器存储和客户端登录状态。
  2. 执行验证码错误或账号重复失败，修正后直接重试。
- 预期结果：
  - HTTP 状态与业务码：成功和失败均返回稳定业务语义。
  - `R<T>` 响应结构：成功不含访问/刷新令牌。
  - 数据、事务和缓存结果：成功后只保留租户和账号；密码、确认密码、验证码均清空。
  - 租户、权限或降级结果：失败保留非敏感输入并刷新验证码，可在当前页面重试。
- 证据：待记录页面截图、存储检查和响应字段。
- 清理：清理测试用户、浏览器会话和临时验证码。
- 实际结果：待执行
- 结果：未执行

### TC-011 普通用户登录、停用删除和管理权限

- 优先级：P0
- 关联需求：REQ-008 / AC-031、AC-032、AC-033、AC-034
- 测试层级：认证 / Saber / 权限集成
- 目标模块与入口：`POST /blade-auth/token`、动态菜单和管理接口
- 依赖：普通角色已配置可访问页面且不含管理权限。
- 前置条件：准备启用、停用和逻辑删除注册用户。
- 测试数据：正确租户、账号和密码。
- 步骤：
  1. 启用用户登录并访问普通页面。
  2. 直接访问用户、角色、租户、菜单、部门和岗位管理接口。
  3. 停用和逻辑删除用户后再次登录。
- 预期结果：
  - HTTP 状态与业务码：启用用户登录成功；管理接口拒绝；停用/删除用户登录失败。
  - `R<T>` 响应结构：失败不签发令牌。
  - 数据、事务和缓存结果：无部门/岗位不阻止登录，也不增加数据范围。
  - 租户、权限或降级结果：菜单不展示管理入口，服务端同样拒绝越权请求。
- 证据：待记录菜单、接口状态和令牌缺失检查。
- 清理：恢复或删除测试用户，清理登录失败计数和客户端会话。
- 实际结果：待执行
- 结果：未执行

### TC-012 网关内部接口隔离与现有流程回归

- 优先级：P0
- 关联需求：REQ-001、REQ-008 / AC-003、AC-031、AC-032
- 测试层级：Gateway / 安全 / 回归
- 目标模块与入口：`/blade-auth/register/**`、`/feign/client/user/register`、现有登录和第三方登录
- 依赖：gateway、auth、system 和 Saber 均已发布。
- 前置条件：准备无令牌、普通令牌和管理员令牌请求。
- 测试数据：公开注册请求和外部 Feign 访问请求。
- 步骤：
  1. 无登录访问注册配置、验证码和注册提交。
  2. 从 gateway 外部直接访问 Feign 注册路径。
  3. 回归已有账号密码登录、第三方登录和管理端用户维护。
- 预期结果：
  - HTTP 状态与业务码：公开注册按业务处理；外部 Feign 请求返回 403；管理接口仍要求认证权限。
  - `R<T>` 响应结构：内部接口拒绝不泄露实现信息。
  - 数据、事务和缓存结果：外部 Feign 绕过请求不创建用户。
  - 租户、权限或降级结果：注册功能关闭时所有现有流程保持兼容。
- 证据：待记录 gateway 响应和回归结果。
- 清理：清理本用例成功创建的测试用户和会话。
- 实际结果：待执行
- 结果：未执行

### TC-013 MySQL 全量与升级脚本

- 优先级：P0
- 关联需求：REQ-003、REQ-007 / AC-009、AC-027、AC-029、AC-030
- 测试层级：MySQL / SQL
- 目标模块与入口：全量脚本、`blade.mysql.upgrade.5.0.1.user-registration.sql`
- 依赖：隔离测试库、备份和可恢复副本。
- 前置条件：准备无冲突库和包含重复/空值异常的冲突库。
- 测试数据：仅使用 `reg20260914_` 前缀的账号和昵称。
- 步骤：
  1. 执行重复账号、空租户、空账号和已有索引检查。
  2. 在无冲突库执行昵称扩容和唯一索引升级。
  3. 核对 `SHOW CREATE TABLE`、索引字段顺序和 32 位默认昵称落库。
  4. 在冲突库验证迁移停止，演练备份恢复和回滚前置条件。
- 预期结果：
  - HTTP 状态与业务码：不适用。
  - `R<T>` 响应结构：不适用。
  - 数据、事务和缓存结果：无冲突库结构与全量脚本一致；冲突数据不被自动删除、合并或改名。
  - 租户、权限或降级结果：唯一索引字段顺序为 `tenant_id, account`，逻辑删除账号仍占用。
- 证据：待记录检查 SQL、DDL、索引和恢复结果，不记录连接凭据。
- 清理：销毁隔离测试库或恢复备份，不操作生产/共享数据库。
- 实际结果：待执行
- 结果：未执行

### TC-014 敏感信息与日志检查

- 优先级：P0
- 关联需求：REQ-003、REQ-006 / AC-012、AC-024
- 测试层级：安全 / 日志 / 浏览器
- 目标模块与入口：Saber、gateway、auth、system 全链路
- 依赖：至少执行成功、参数失败、验证码失败、限流、重复和 Feign 失败场景。
- 前置条件：启用必要的测试日志，但不得开启打印完整敏感请求体的配置。
- 测试数据：临时测试账号；密码、密文、摘要、验证码和令牌不写入测试记录。
- 步骤：检查浏览器状态、接口响应、控制台、gateway/auth/system 日志和普通缓存。
- 预期结果：
  - HTTP 状态与业务码：各场景保持稳定业务语义。
  - `R<T>` 响应结构：不出现密码、确认密码、SM2 密文、摘要、验证码或令牌。
  - 数据、事务和缓存结果：MySQL 仅存密码摘要，不新增注册申请、审计或验证码表。
  - 租户、权限或降级结果：日志可定位租户、账号、结果和请求标识，但不记录敏感请求体。
- 证据：待记录脱敏后的日志检查结论和匹配数量。
- 清理：清理测试用户、Redis 临时键和客户端会话。
- 实际结果：待执行
- 结果：未执行

### TC-015 SystemApplication 启动与 Bean 依赖回归

- 优先级：P0
- 关联需求：REQ-002、REQ-005 / AC-006、AC-017、AC-020
- 测试层级：ApplicationContext / 启动冒烟
- 目标模块与入口：`blade-service/blade-system`、`SystemApplication`
- 依赖：测试环境 Nacos、MySQL、Redis 等 system 启动依赖已准备。
- 前置条件：使用包含本需求变更的同一版本 API 和 system 构建产物，保留 Spring Boot 默认禁止循环依赖配置。
- 测试数据：不涉及业务写入。
- 步骤：
  1. 启动 `SystemApplication` 并等待应用上下文初始化完成。
  2. 检查 `TenantServiceImpl` 不依赖 `IUserService`，`UserServiceImpl` 通过 `ITenantService` 校验租户，`TenantProvisionServiceImpl` 单向编排租户、角色、部门、岗位和用户服务。
  3. 检查启动日志中的 `TenantProvisionServiceImpl`、`TenantServiceImpl`、`UserServiceImpl` 和 `TenantController` Bean 创建结果。
  4. 调用一个只读健康检查或 system 只读接口，确认应用已正常对外提供服务。
- 预期结果：
  - HTTP 状态与业务码：健康检查或只读接口正常响应。
  - `R<T>` 响应结构：沿用目标接口既有结构。
  - 数据、事务和缓存结果：启动过程不写入注册、租户、角色或用户数据。
  - 租户、权限或降级结果：依赖方向为 `TenantProvisionServiceImpl -> UserServiceImpl -> TenantServiceImpl`，没有反向依赖，不需要开启 `spring.main.allow-circular-references`。
- 证据：待记录启动完成日志和只读请求响应，不记录连接凭据。
- 清理：正常停止测试实例，无业务数据需要清理。
- 实际结果：待执行
- 结果：未执行

## 5. 专项检查

- [ ] 参数缺失、格式错误、租户不存在、角色缺失/重复和验证码失败。
- [ ] 未登录公开入口、外部 Feign 拒绝、无权限管理接口和越租户访问。
- [ ] 重复请求、并发注册、事务回滚和数据库唯一索引。
- [ ] Feign 正常调用、业务失败、超时/Fallback 及 auth 调用方判断。
- [ ] MySQL 全量脚本、升级脚本、冲突停止、结构核对和回滚演练。
- [ ] 用户缓存失效、缓存异常和后续查询回源。
- [ ] 响应、日志、浏览器状态和普通缓存不泄露敏感信息。
- [ ] `SystemApplication` 默认配置可启动，租户服务与用户服务不存在循环依赖。

## 6. 验收覆盖矩阵

| 验收标准 | 测试用例 | 结果 | 说明 |
| --- | --- | --- | --- |
| AC-001~003 | TC-001、TC-012 | 未执行 | 注册开关、入口、直接请求和网关 |
| AC-004~007 | TC-002、TC-003、TC-015 | 未执行 | 默认/显式租户、无效租户、隔离和启动依赖 |
| AC-008~012 | TC-002、TC-004、TC-014 | 未执行 | 输入、昵称、密码和敏感信息 |
| AC-013~016 | TC-005、TC-006 | 未执行 | 验证码与频率限制 |
| AC-017~021 | TC-002、TC-004、TC-007、TC-009、TC-015 | 未执行 | 普通角色、受控字段、事务、组织边界和启动依赖 |
| AC-022~025 | TC-002、TC-009、TC-010、TC-014 | 未执行 | 回登录、状态清理、无令牌和重试 |
| AC-026~030 | TC-003、TC-008、TC-013 | 未执行 | 唯一性、跨租户、逻辑删除和并发 |
| AC-031~034 | TC-002、TC-011、TC-012 | 未执行 | 普通用户登录、权限、无部门岗位和停用删除 |

## 7. 缺陷与汇总

| 缺陷编号 | 关联用例 | 严重级别 | 描述 | 状态 |
| --- | --- | --- | --- | --- |
| DEFECT-001 | TC-015 | 阻断 | 租户初始化与用户注册校验曾形成 `TenantServiceImpl`、`UserServiceImpl` 双向依赖，导致 system 应用上下文启动失败 | 应用编排重构及 JDK 21 模块打包通过，待启动验证 |

| 指标 | 数量 |
| --- | ---: |
| 用例总数 | 15 |
| 通过 | 0 |
| 失败 | 0 |
| 阻塞 | 0 |
| 不适用 | 0 |
| 未执行 | 15 |

- 测试结论：待执行
- 未完成项：真实 MySQL、Redis、Nacos、Feign、gateway 和 Saber 联调验收。
- 遗留风险：租户普通角色配置、历史账号冲突、无部门岗位业务兼容性和限流阈值仍需真实环境确认。
- 数据清理结果：待执行。

## 8. 变更记录

| 日期 | 版本 | 变更内容 | 修改人 |
| --- | --- | --- | --- |
| 2026-09-14 | 0.1 | 创建用户自助注册测试文档，覆盖 34 项验收标准及后端、前端、数据库、网关和安全检查 | Codex |
| 2026-09-15 | 0.2 | 增加 `SystemApplication` 启动与 Bean 依赖回归用例，记录并跟踪租户服务与用户服务循环依赖缺陷 | Codex |
| 2026-09-15 | 0.3 | 更新启动回归断言，验证租户开通编排服务与租户、用户实体服务保持单向依赖；JDK 21 关联模块打包通过 | Codex |
| 2026-09-15 | 0.4 | 增加注册租户编号类型检查，验证请求、配置和响应使用 `tenant_id` 字符串并保留 `000000` 前导零 | Codex |
