# 图片提示词反推与标签化分析需求文档

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 需求名称 | 图片提示词反推与标签化分析 |
| 需求编号 | REQ-2026-004 |
| 文档版本 | 0.4 |
| 所属模块 | AI 分析、租户标签快照、提示词生成 |
| 目标版本/迭代 | SpringBlade 5.x / AI 能力第二阶段 |
| 文档状态 | 开发中 |
| 产品负责人 | 待指定 |
| 技术负责人 | 待指定 |
| 创建日期 | 2026-09-15 |
| 最后更新日期 | 2026-09-17 |
| 关联事项 | [需求索引](../requirements-index.md)；[详细设计](../design/DESIGN-REQ-2026-004-image-prompt-reverse.md)；[测试文档](../test/TEST-REQ-2026-004-image-prompt-reverse.md)；数据库设计不涉及；fast-staratlas-ai：REQ-2026-011；Saber：REQ-2026-013；现有提示词管理：REQ-2026-001；标签分类管理：REQ-2026-002；提示词类型、发布方式与数据权限：REQ-2026-005 |

## 2. 摘要与目标

### 2.1 摘要

为租户用户提供图片提示词反推能力。用户只上传图片，系统调用 `fast-staratlas-ai` 分析画面内容，从当前租户的有效标签体系中选择标签，并返回固定 JSON、正向提示词、负向提示词和必要的自由描述，方便用户复制后继续生成图片。

本需求不是恢复图片生成时的原始提示词，而是基于图片内容生成可复用、可验证、标签受控的提示词结果。

### 2.2 需求目标

1. 用户只需上传一张图片即可发起分析，不要求用户预先填写标签或 JSON。
2. 系统只能从当前租户有效标签中选取标签，不得生成租户不存在的标签编码。
3. 标签用于稳定控制，主体、场景、物体、动作和细节通过自由描述补充，避免只依赖标签造成信息损失。
4. 返回固定版本化 JSON，包含标签、视觉分析、正向提示词、负向提示词和警告。
5. 通过 Redis 共享标签快照减少每次请求传输完整标签体系，并支持多实例部署和分布式锁。
6. 复用更新后的提示词类型、发布方式、不可变版本和 DataScope 能力，将 AI 分析规则固化为 `SYSTEM + MANUAL` 的可升级、可回滚和可追溯策略提示词。

### 2.3 非目标

- 不承诺从图片像素精确恢复原始提示词、模型、LoRA、Seed、采样器或 CFG 等生成参数。
- 不要求用户提供标签、固定 JSON、来源元数据或原始提示词。
- 不在本期持久化上传图片、分析历史、提示词结果或模型响应。
- 不新增 `blade_ai_prompt` 表结构；直接复用现有提示词类型、发布方式、版本和权限能力。
- 不为分析策略另建 Redis 当前指针或第二套提示词发布状态；MySQL 当前发布版本仍是提示词运行时事实来源。
- 不在本期建设模型供应商管理、模型效果评分、批量图片分析或异步任务中心。
- 不让浏览器直接访问 `fast-staratlas-ai` 或持有其 API Key。

## 3. 用户故事与范围

### 3.1 用户故事

| 故事编号 | 优先级 | 用户故事 | 典型场景 | 依赖 |
| --- | --- | --- | --- | --- |
| US-001 | Must | 作为租户用户，我希望上传图片后自动分析，以便快速获得可用的生图提示词。 | 资产管理中的图片反推入口 | 用户登录、AI 分析服务 |
| US-002 | Must | 作为租户用户，我希望系统只使用当前租户已有标签，以便分析结果可以被业务统一使用。 | 返回内容类型、风格、构图、光线和色彩标签 | REQ-2026-002 |
| US-003 | Must | 作为租户用户，我希望同时看到标签和自由描述，以便结果既稳定又保留画面细节。 | 复制正向提示词或查看结构化 JSON | US-001、US-002 |
| US-004 | Must | 作为维护者，我希望标签变更后分析结果使用明确版本，以便追溯结果依据。 | 标签停用或新增后的分析请求 | Redis 标签快照 |
| US-005 | Should | 作为租户用户，我希望看到不确定项和警告，以便知道哪些内容不能被模型确认。 | 图片细节不清、标签体系未覆盖 | AI 分析结果 |

### 3.2 范围内

- 在 `blade-ai` 增加受保护的图片反推入口。
- 从 `blade-system` 获取当前租户有效标签分类和标签树快照。
- 生成规范化、不可变的标签快照并计算版本摘要。
- 使用 Redis 共享标签快照、当前版本指针和构建锁。
- 首期以 Redis 为标签快照的共享读取路径，不以各应用实例的进程内缓存承载当前快照。
- 复用现有提示词管理，以稳定编码 `image_prompt_reverse`、类型 `SYSTEM`、发布方式 `MANUAL` 管理 AI 分析策略。
- `blade-ai` 在每次反推请求中读取并渲染当前发布版本，将渲染消息和版本信息传给 fast，不新增提示词 Redis 双写。
- 每个租户在开放图片反推前由初始化或运维流程创建并发布管理员所有的 `image_prompt_reverse`；普通 `OWN` 用户不维护该系统策略。
- 服务端调用 `fast-staratlas-ai` 完成视觉分析和提示词生成。
- 校验返回标签必须属于本次标签快照，并遵守分类单选、多选和最大数量规则。
- 返回固定 JSON、标签置信度、自由描述、正向提示词、负向提示词和警告。
- 通过权限码控制图片反推能力，按当前租户隔离标签体系。

### 3.3 范围外

- 标签与图片资源的持久化绑定、收藏、历史版本和回溯页面。
- 跨租户标签共享或平台公共标签库。
- 自动创建或修改标签；发现标签体系覆盖不足时只返回未匹配视觉特征。
- 将结果自动发布到提示词管理模块。
- 允许浏览器或普通调用方提交任意系统指令、分析步骤或覆盖已发布的分析提示词。
- 将 `image_prompt_reverse` 改为 `AUTO` 发布；系统分析规则必须经过独立验证和手工发布。
- 通过图片内嵌元数据恢复原始生图参数。

## 4. 业务流程

### 4.1 流程说明

- 前置条件：用户已登录当前租户，并具备图片反推权限；当前租户存在由初始化或管理员维护的、类型为 `SYSTEM`、发布方式为 `MANUAL`、已发布且未停用的 `image_prompt_reverse`；`fast-staratlas-ai` 和 Redis 可用。
- 触发入口：Saber 资产管理/提示词管理页面的图片反推入口。
- 最终结果：用户获得固定 JSON 和可复制提示词；系统不保存图片和本次分析结果。

1. 用户选择一张符合格式和大小限制的图片并提交分析。
2. `blade-ai` 校验身份、租户、权限、文件格式和文件大小。
3. 系统取得当前租户有效标签快照；快照不存在时按租户分布式锁生成并写入 Redis。
4. `blade-ai` 按稳定编码读取当前发布版本，校验类型和发布方式，并使用现有运行时渲染组件生成 `SYSTEM`/`USER` 文本消息。
5. 系统将图片、`taxonomyHash`、渲染消息、提示词 `versionId/versionNo/contentHash` 转发给 `fast-staratlas-ai`；浏览器不能提交这些内部字段。
6. AI 服务从 Redis 读取标签快照，将受信任的渲染消息、标签定义和图片组合后调用视觉模型。
7. `blade-ai` 二次校验标签归属和选择数量，返回固定 JSON、标签快照 hash 和提示词版本信息。

### 4.2 业务流程图

~~~mermaid
flowchart TD
    A([开始]) --> B[用户上传图片]
    B --> C{已登录且有图片反推权限?}
    C -- 否 --> D[返回认证或权限错误]
    D --> Z([结束])
    C -- 是 --> E{图片格式和大小合法?}
    E -- 否 --> F[返回文件校验错误]
    F --> Z
    E -- 是 --> G[读取当前租户标签快照]
    G --> H{Redis 快照命中?}
    H -- 否 --> I[加分布式锁并从标签服务构建快照]
    I --> J[写入 Redis 并取得 taxonomyHash]
    H -- 是 --> J
    J --> O{SYSTEM和MANUAL策略已发布?}
    O -- 否 --> P[返回分析策略不可用]
    P --> Z
    O -- 是 --> Q[渲染当前版本并取得版本信息]
    Q --> K[携带图片、taxonomyHash和渲染消息调用fast]
    K --> L{返回 JSON 和标签合法?}
    L -- 否 --> M[返回分析失败或重试提示]
    M --> Z
    L -- 是 --> N[返回标签、分析、正向/负向提示词]
    N --> Z
~~~

### 4.3 异常与替代流程

| 编号 | 触发条件 | 系统行为 | 用户提示 | 数据是否改变 |
| --- | --- | --- | --- | :---: |
| EX-001 | 未登录或无图片反推权限 | 拒绝请求 | 无权限使用图片反推 | 否 |
| EX-002 | 文件为空、格式不支持或超过大小 | 拒绝请求 | 请上传符合要求的图片 | 否 |
| EX-003 | 当前租户没有有效标签 | 不调用模型 | 请先配置有效标签体系 | 否 |
| EX-004 | 标签快照构建锁竞争或 Redis 不可用 | 不使用不确定的标签体系 | 标签服务暂不可用，请重试 | 否 |
| EX-005 | fast-staratlas-ai 超时或不可用 | 返回明确依赖失败 | 图片分析服务暂不可用 | 否 |
| EX-006 | 模型返回未知标签或违反选择规则 | 校验失败，可进行一次结构修复 | 分析结果无法校验，请重试 | 否 |
| EX-007 | 图片内容无法可靠判断某项 | 保留自由描述中的不确定说明，不强行补标签 | 部分内容无法确认 | 否 |
| EX-008 | 分析提示词不存在、未发布或已停用 | 不调用 fast 和视觉模型 | 图片分析规则未发布或不可用 | 否 |
| EX-009 | 分析提示词类型不是 `SYSTEM`、发布方式不是 `MANUAL` 或渲染失败 | 拒绝调用 fast，不使用草稿或错误类型替代 | 图片分析规则配置错误 | 否 |
| EX-010 | 当前租户尚未初始化管理员所有的保留提示词 | 拒绝分析，不允许普通用户临时创建同编码策略 | 请联系管理员初始化图片分析规则 | 否 |

## 5. 功能需求与验收标准

### 5.1 REQ-001 图片反推入口与请求校验

- 关联用户故事：`US-001`
- 优先级：Must
- 触发入口：图片反推页面提交操作。
- 前置条件：用户已登录当前租户并具备图片反推权限。
- 处理规则：
  1. 请求必须包含一张图片。
  2. 服务端校验实际文件类型，不以扩展名作为唯一依据。
  3. 单次只分析一张图片，超出大小或格式范围直接拒绝。
  4. 客户端不能指定 tenantId、标签快照内容或模型配置。
  5. 文件大小上限统一使用环境生效的 Spring `spring.servlet.multipart.max-file-size`，不维护图片反推专属文件大小配置。
- 验收标准：
  - `AC-001`：Given 用户已登录且有权限，When 上传合法图片，Then 系统进入分析流程并返回结果或明确的依赖失败。
  - `AC-002`：Given 用户未登录或无权限，When 提交图片，Then 返回统一认证/权限失败且不调用 AI 服务。
  - `AC-003`：Given 文件为空、格式不支持或超过大小限制，When 提交分析，Then 请求被拒绝且不改变任何数据。

### 5.2 REQ-002 当前租户标签快照

- 关联用户故事：`US-002`、`US-004`
- 优先级：Must
- 触发入口：图片反推流程内部。
- 处理规则：
  1. 标签快照只包含当前租户有效分类和有效标签。
  2. 快照包含分类选择模式、最大数量、标签编码、名称、说明和父级路径。
  3. 快照按规范化内容计算 `taxonomyHash`，同一内容必须得到同一 hash。
  4. 标签快照按 hash 作为不可变版本保存，当前租户通过指针指向当前 hash。
  5. 快照构建使用租户级分布式锁，避免多个实例重复查询和写入。
  6. 标签变更提交后，后续分析必须能够使用新的快照；正在执行的请求可以继续使用已取得的快照。
- 验收标准：
  - `AC-004`：Given 当前租户有有效标签，When 首次发起分析，Then 系统生成标签快照并得到 `taxonomyHash`。
  - `AC-005`：Given 多个实例同时首次构建同一租户快照，When 并发发起分析，Then 只允许一个构建流程写入，其他请求复用结果或等待后读取。
  - `AC-006`：Given 标签被新增、停用或修改，When 下一次分析使用当前标签体系，Then 结果依据新的快照版本。

### 5.3 REQ-003 受控视觉分析与标签选择

- 关联用户故事：`US-002`、`US-003`
- 优先级：Must
- 触发入口：标签快照准备完成后的 AI 分析。
- 处理规则：
  1. AI 可以生成自由视觉描述，但标签只能从本次快照中选择。
  2. 单选分类最多返回一个标签，多选分类不得超过最大数量。
  3. 未知标签编码、停用标签、跨分类标签和超限结果必须被判为无效。
  4. 标签无法覆盖的主体、物体、动作和细节写入自由分析字段，不创建临时标签。
  5. 模型结果无法通过结构校验时最多执行一次修复；仍失败则返回明确错误。
- 验收标准：
  - `AC-007`：Given 图片分析返回的所有标签都存在于当前快照，When 处理结果，Then 返回合法标签及其置信度。
  - `AC-008`：Given 模型返回未知或停用标签，When 服务端校验，Then 拒绝该结果，不把未知标签返回给用户。
  - `AC-009`：Given 某分类配置为单选或限制最大数量，When 模型返回超限标签，Then 结果校验失败且不返回超限数据。

### 5.4 REQ-004 固定 JSON 与提示词结果

- 关联用户故事：`US-003`、`US-005`
- 优先级：Must
- 处理规则：
  1. 返回 JSON 必须包含 `schemaVersion`、`taxonomyHash`、视觉分析、标签、正向提示词、负向提示词和警告。
  2. 标签使用稳定的 `categoryCode` 和 `tagCode`，同时返回当前名称用于展示。
  3. 正向提示词必须结合受控标签和自由描述，不能只拼接标签名称。
  4. 无法从图片确认的模型参数不得伪造，应放入警告或未知项。
  5. 结果只作为本次响应返回，不自动写入提示词管理数据；提示词策略版本信息只用于追溯。
- 验收标准：
  - `AC-010`：Given AI 分析成功，When 返回响应，Then JSON 字段结构固定、版本明确且包含正向/负向提示词。
  - `AC-011`：Given 标签体系未覆盖某个视觉细节，When 分析图片，Then 细节可以保留在自由描述或提示词中，不生成未登记标签。
  - `AC-012`：Given 用户查看结果，When 复制正向提示词或完整 JSON，Then 内容与响应一致且不包含密钥、Token 或内部连接信息。

首期固定 JSON 结构基线：

```json
{
  "schemaVersion": "1.0",
  "taxonomyHash": "sha256:...",
  "analysis": {
    "summary": "图片整体内容摘要",
    "subject": "主体、外观、动作和关系描述",
    "scene": "环境、时间、空间和背景描述",
    "details": ["材质、服饰、物体等可观察细节"],
    "unmatchedFeatures": ["当前标签体系未覆盖但提示词需要保留的特征"],
    "uncertainFeatures": ["模型无法可靠确认的内容"]
  },
  "labels": [
    {
      "categoryCode": "lighting",
      "categoryName": "光线特征",
      "selections": [
        {
          "tagCode": "neon_light",
          "tagName": "霓虹光",
          "confidence": 0.94,
          "reason": "画面主要由彩色发光体照明"
        }
      ]
    }
  ],
  "prompt": {
    "language": "en",
    "positive": "positive prompt",
    "negative": "negative prompt",
    "segments": {
      "subject": [],
      "scene": [],
      "style": [],
      "composition": [],
      "lighting": [],
      "color": [],
      "quality": []
    }
  },
  "warnings": [
    {
      "code": "generation_parameters_unknown",
      "message": "无法从图片确认原始模型、Seed、采样器和 CFG"
    }
  ]
}
```

`labels` 中没有命中的分类允许不返回；`segments` 用于解释最终提示词的组成，不代替 `positive` 和 `negative`。详细设计可以补充字段长度和枚举，但不得改变上述字段的基本语义。

成功响应还必须包含本次使用的分析提示词版本。该信息来自现有提示词运行时渲染结果，不是 Redis 中的第二份提示词快照：

```json
{
  "analysisPrompt": {
    "code": "image_prompt_reverse",
    "promptType": "SYSTEM",
    "versionId": "220260916000000001",
    "versionNo": 3,
    "contentHash": "0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"
  }
}
```

`schemaVersion`、`analysisPrompt.versionId/versionNo/contentHash` 和 `taxonomyHash` 是三个不同概念：Schema 版本控制响应结构，已发布提示词版本控制 AI 行为，标签快照控制可选标签。`versionId` 是发布版本身份，`contentHash` 是现有提示词正文完整性校验值；普通提示词发布不得擅自修改固定 JSON 字段，字段结构变化必须升级 `schemaVersion` 并同步修改后端和前端契约。

### 5.5 REQ-005 服务依赖与错误语义

- 关联用户故事：`US-004`、`US-005`
- 优先级：Must
- 处理规则：
  1. `blade-ai` 服务端持有 fast 服务凭证，客户端不能提交或覆盖模型档案、服务地址和 API Key。
  2. Redis、标签服务、当前发布提示词或 fast 服务不可用时，返回可区分的失败结果，不降级为无标签自由生成。
  3. 普通日志只记录租户、请求标识、标签快照 hash、提示词版本号、耗时和错误码，不记录图片正文和完整提示词。
- 验收标准：
  - `AC-013`：Given fast 服务超时或返回依赖失败，When 用户提交分析，Then 页面显示可重试错误，不显示成功结果。
  - `AC-014`：Given Redis 不可用且无法取得标签快照，When 用户提交分析，Then 系统拒绝分析，不使用过期或未知标签体系冒充成功。
  - `AC-015`：Given 开启普通业务日志，When 分析成功或失败，Then 日志不包含图片二进制、API Key、Token 和完整提示词正文。

### 5.6 REQ-006 系统分析策略提示词

- 关联用户故事：`US-001`、`US-004`、`US-005`
- 优先级：Must
- 触发入口：现有提示词管理的草稿、预览、发布和回滚流程；图片反推运行时读取。
- 处理规则：
  1. AI 分析策略使用稳定编码 `image_prompt_reverse`，类型固定为 `SYSTEM`，发布方式固定为 `MANUAL`，编码创建后不可修改。
  2. 固定指令描述角色、分析目标、处理步骤、标签约束、防编造规则、提示词生成规则和固定 JSON 输出要求。
  3. 具体标签列表不写入分析提示词，由独立 `taxonomyHash` 标签快照提供。
  4. 每个租户在开放图片反推前必须初始化该保留编码；普通 `OWN` 用户不得创建、复制、编辑、发布、停用或删除该系统策略。
  5. 图片反推只使用当前租户已发布且未停用的版本，不读取草稿或 `AUTO` 版本。
  6. `blade-ai` 直接复用当前发布版本的运行时渲染能力，将渲染后的消息传给 fast；不为分析提示词建立第二套 Redis 内容快照。
  7. 新请求使用新的当前版本；已经取得旧版本渲染结果的进行中请求继续完成，不在请求中途切换规则。
  8. 回滚沿用现有语义：基于历史版本生成新的发布版本，不覆盖历史记录。
  9. 每次响应返回提示词编码、类型、版本 ID、版本号和 `contentHash`，用于质量追溯。
- 访问保护：
  1. 系统策略管理需要独立的系统提示词管理权限，且数据范围必须为当前租户 `ALL`。
  2. 图片反推使用权限与系统策略管理权限分离；拥有 `ai:prompt:reverse` 不等于可以编辑系统分析提示词。
  3. 运行时调用只读取当前租户当前发布版本，不应用管理列表的 OWN 数据范围过滤。
- 验收标准：
  - `AC-016`：Given `image_prompt_reverse` 为 `SYSTEM + MANUAL` 且已有当前发布版本，When 用户发起图片分析，Then 系统使用当前发布版本且响应包含其版本 ID、版本号和 `contentHash`。
  - `AC-017`：Given 该提示词只有草稿、未发布、已停用、类型错误或发布方式为 AUTO，When 用户发起分析，Then 系统拒绝请求且不调用视觉模型。
  - `AC-018`：Given 普通 OWN 用户拥有通用提示词管理权限，When 其尝试创建或修改 `image_prompt_reverse`，Then 系统拒绝且不能改变系统策略。
  - `AC-019`：Given 系统提示词管理员发布新版本，When 发布完成后发起新请求，Then 新请求使用新版本，历史响应仍可通过版本 ID 和 `contentHash` 追溯。
  - `AC-020`：Given 新版本效果异常，When 系统提示词管理员回滚历史内容，Then 系统生成新的发布版本，后续请求使用回滚内容且历史版本不被修改。

## 6. 业务规则与状态

### 6.1 业务规则

| 规则编号 | 规则 | 适用范围 | 违反时行为 |
| --- | --- | --- | --- |
| BR-001 | 图片反推只接受当前登录用户所属租户的请求 | 入口、标签快照 | 认证或权限失败 |
| BR-002 | 标签真实来源是当前租户有效标签，Redis 只保存快照缓存 | 标签快照 | 快照缺失时重新构建，不使用硬编码副本 |
| BR-003 | `taxonomyHash` 标识不可变标签快照内容 | 请求、结果、追溯 | hash 与内容不一致时拒绝使用 |
| BR-004 | 标签结果必须满足分类选择模式和最大数量 | AI 结果校验 | 结构校验失败 |
| BR-005 | 自由描述可以包含未被标签体系覆盖的细节 | JSON、提示词 | 不创建临时标签 |
| BR-006 | 未知模型参数不得被声称为原始事实 | 结果说明 | 放入未知项或警告 |
| BR-007 | 本需求不保存图片和分析历史 | 数据边界 | 不新增持久化记录 |
| BR-008 | 标签快照主路径直接读取 Redis，不依赖进程内缓存保持多实例一致 | 标签读取 | Redis 不可用时明确失败 |
| BR-009 | AI 分析策略必须使用当前租户 `image_prompt_reverse` 的已发布版本，草稿不得进入运行时 | 分析提示词 | 未发布或停用时拒绝分析 |
| BR-010 | `image_prompt_reverse` 必须是 `SYSTEM + MANUAL`，普通 OWN 用户不能维护 | 策略管理 | 拒绝操作且不改变数据 |
| BR-011 | 分析策略使用现有 MySQL 发布版本和运行时渲染，不建立第二套提示词 Redis 快照 | 运行时读取 | 按当前发布版本读取，Redis 仅用于标签快照 |
| BR-012 | 结果使用 `versionId + versionNo + contentHash` 追溯分析策略，版本 ID 表示身份，contentHash 用于正文完整性核对 | 响应、日志、追溯 | 版本信息缺失时结果不完整 |
| BR-013 | 提示词升级不得在同一 `schemaVersion` 下破坏固定 JSON 字段语义 | 输出契约 | 结构变化必须升级 Schema 并同步调用方 |

### 6.2 状态与迁移

不建立持久化任务状态。单次请求只返回成功或明确失败；异步任务、取消、恢复和历史状态作为后续需求处理。

## 7. 认证与访问边界

### 7.1 资源归属

| 资源 | 归属类型 | 归属字段 | 字段来源 | 说明 |
| --- | --- | --- | --- | --- |
| 图片反推请求 | 租户级 | 当前租户上下文 | SpringBlade 安全上下文 | 不落库，仅在请求期间使用 |
| 标签快照当前指针 | 租户级 | `tenant_id` | 服务端上下文 | 只能读取当前租户标签 |
| 标签快照内容 | 由租户快照 hash 标识 | hash/快照索引 | 服务端生成 | 只读版本，不接受客户端指定内容 |
| 分析策略提示词 | 租户级系统策略 | `tenant_id` + `prompt_code` | 现有提示词管理 | `image_prompt_reverse` 固定为 SYSTEM + MANUAL |
| 分析策略渲染结果 | 请求级 | `versionId`、`versionNo`、`contentHash` | `blade-ai` 当前发布版本 | 仅随请求传给 fast，不持久化 |

### 7.2 当前访问规则

| 操作 | 是否需要登录 | 业务前置条件 | 失败行为 |
| --- | :---: | --- | --- |
| 发起图片反推 | 是 | 有 `ai:prompt:reverse` 权限，图片合法 | 401/403/业务错误 |
| 读取当前租户标签快照 | 是 | 由图片反推服务内部调用 | 越租户或不可用时失败 |
| 写入标签快照缓存 | 否（用户不可直接调用） | 由服务端在标签变更或缓存缺失时执行 | 返回依赖失败，不开放客户端写入 |
| 管理分析提示词 | 是 | 复用现有提示词查看、编辑、发布、停用和回滚权限 | 沿用 REQ-2026-001 错误语义 |
| 读取分析策略 | 否（用户不可直接调用） | 图片反推内部读取当前发布版本并渲染 | 缺失、未发布、停用或类型错误时失败 |

### 7.3 后续权限影响

- 权限资源：`ai:prompt:reverse`。
- 系统策略管理权限：`ai:prompt:system-manage`，与普通提示词编辑、发布权限分离。
- 内部标签读取权限：`system:tag:runtime`，用于具备图片反推权限的服务调用链。
- 作用域：当前租户级。
- 当前不新增独立角色；由部署环境决定哪些角色拥有图片反推权限。

## 8. 页面与交互要求

本需求定义后端能力，Saber 页面由 REQ-2026-013 详细规定。后端必须返回可区分的加载中、依赖失败、标签校验失败和成功结果语义。

## 9. 质量要求与影响

### 9.1 非功能要求

| 类别 | 要求 | 验证标准 |
| --- | --- | --- |
| 性能 | 不要求每次分析请求传输完整标签体系；命中共享标签快照后只传递 hash，分析策略正文由 blade-ai 渲染一次 | 多实例请求抓包和 Redis 命中验证 |
| 并发 | 同一租户快照构建必须有分布式锁，不能产生重复构建和不一致指针 | 并发构建用例 |
| 安全 | 浏览器不接触 fast API Key；日志不记录图片和完整提示词 | 安全与日志检查 |
| 一致性 | MySQL 提交成功后才更新/失效快照；结果返回快照 hash | 标签修改后版本验证 |
| 可追溯性 | 每次结果返回分析提示词版本 ID、版本号、contentHash 和标签快照 hash | 发布、升级、回滚后的响应核对 |
| 可用性 | Redis、标签服务、fast 服务失败时明确失败，不返回伪造成功 | 依赖故障用例 |

### 9.2 数据与接口影响

| 影响项 | 是否涉及 | 需求层说明 | 关联设计 |
| --- | :---: | --- | --- |
| 新增/修改数据实体 | 否 | 不新增业务表；复用现有提示词表，Redis 只保存标签快照 | 不涉及 |
| 新增/修改 API | 是 | 已新增图片反推接口、标签运行时快照 Feign 契约；`PromptRenderVO` 增加 `contentHash` | [详细设计](../design/DESIGN-REQ-2026-004-image-prompt-reverse.md) |
| 历史数据处理 | 否 | 不改变现有提示词、标签和历史数据 | 不涉及 |
| 配置或部署变化 | 是 | 已新增 fast 服务地址、凭证、超时、Redis 标签快照命名空间和保留系统提示词初始化；文件大小沿用 Spring multipart 配置 | [详细设计](../design/DESIGN-REQ-2026-004-image-prompt-reverse.md) |
| 外部依赖 | 是 | 依赖 fast-staratlas-ai、Redis、配置的视觉模型档案和 WebP ImageIO 解码器 | [详细设计](../design/DESIGN-REQ-2026-004-image-prompt-reverse.md) |

### 9.3 依赖、风险与开放问题

| 编号 | 类型 | 内容 | 负责人 | 截止日期 | 状态/结论 |
| --- | --- | --- | --- | --- | --- |
| ITEM-001 | 依赖 | fast-staratlas-ai 需要支持受控标签和固定 JSON 输出 | 待指定 | 待指定 | 开放 |
| ITEM-002 | 风险 | 标签体系规模持续增长后可能增加模型输入长度和成本 | 待指定 | 待指定 | 首期使用快照，后续增加候选标签检索 |
| ITEM-003 | 问题 | Redis 是否由 SpringBlade 与 fast 服务共享集群及 ACL 方案待部署确认；Redis 只承载标签快照 | 待指定 | 待指定 | 开放 |
| ITEM-004 | 风险 | 模型对中文标签说明、复杂主体关系和小字文字的识别能力存在差异 | 待指定 | 待指定 | 通过固定评测集验收 |
| ITEM-005 | 问题 | 正向/负向提示词默认语言及目标模型配置待确认 | 待指定 | 待指定 | 建议首期英文提示词，中文说明并行返回 |
| ITEM-006 | 风险 | 分析提示词升级可能改善部分图片但降低其他图片效果 | 待指定 | 待指定 | 发布前使用固定图片评测集比较，保留快速回滚能力 |
| ITEM-007 | 依赖 | 每个租户需要初始化管理员所有的 `image_prompt_reverse` 系统提示词 | 待指定 | 待指定 | 开放；未初始化时拒绝图片分析 |

## 10. 评审与变更

### 10.1 需求评审清单

- [x] 摘要、目标、非目标和范围已明确。
- [x] 用户故事可在目标迭代内完成。
- [x] 业务流程图与功能需求一致，并包含主要失败分支。
- [x] 每项 Must 需求都有可测试的验收标准。
- [x] 业务规则、状态和异常行为已初步明确。
- [x] 认证要求和数据归属已明确；权限码和初始化 SQL 已登记。
- [x] 数据库、API、Redis、配置和外部服务影响已识别。
- [x] 详细设计和测试文档草稿已建立；评审结论待补充。

### 10.2 评审结论

| 评审领域 | 结论 | 评审人 | 日期 | 条件/备注 |
| --- | --- | --- | --- | --- |
| 产品范围 | 待评审 | 待指定 | 待指定 | 需确认首期模型、语言和页面入口 |
| 技术可行性 | 待评审 | 待指定 | 待指定 | 需确认 Redis 共享与内部 Feign 方案 |
| 测试可验收性 | 待评审 | 待指定 | 待指定 | 需准备带标准标签的图片评测集 |

### 10.3 变更记录

| 日期 | 版本 | 变更内容 | 原因 | 影响范围 | 修改人 |
| --- | --- | --- | --- | --- | --- |
| 2026-09-15 | 0.1 | 初稿。建立图片分析、租户标签快照、固定 JSON、提示词生成和 Redis 共享缓存范围。 | 用户确认只上传图片，由系统基于现有标签体系完成分析和提示词生成 | SpringBlade AI 接口、标签 Feign、Redis 快照、fast 调用方 | Codex |
| 2026-09-15 | 0.2 | 将 AI 分析规则纳入现有提示词管理，以 `image_prompt_reverse` 稳定编码发布和回滚；新增 Redis 分析提示词快照、`analysisPromptHash`、版本追溯及 Schema 兼容边界。 | 用户确认分析指令应固化并可随时升级 | 提示词运行时、Redis 快照、fast 请求、固定 JSON 和验收标准 | Codex |
| 2026-09-16 | 0.3 | 按提示词管理增强重新评估：分析策略固定为 `SYSTEM + MANUAL`，直接读取当前发布版本并由运行时渲染；移除分析提示词 Redis 双写和 `analysisPromptHash`，改用 `versionId + contentHash` 追溯；增加系统策略保留编码、初始化和管理员 ALL 范围。 | 提示词管理已具备类型、发布方式、版本、回滚和 DataScope 能力 | 分析流程、权限边界、Redis 范围、响应追溯和验收标准 | Codex |
| 2026-09-17 | 0.4 | 进入 SpringBlade 后端实现，明确文件大小统一复用 Spring multipart 配置，不设置业务专属上限；完成接口、策略保护、标签快照、fast 调用、输出校验和权限 SQL。 | 用户要求开始实现，并确认文件上传统一使用 Spring 配置 | AI/System API、blade-ai、blade-system、Redis、权限初始化和部署配置 | Codex |
