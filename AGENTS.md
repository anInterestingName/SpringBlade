# AGENTS.md

本文件适用于仓库根目录及全部子目录，供参与 SpringBlade 开发的 AI 代理和工程师使用。交流、文档、代码注释和提交说明默认使用中文，代码标识符保持英文。规范与实际代码不一致时，以用户当前要求、已确认需求和当前代码事实为准。

## 1. 项目定位与事实来源

SpringBlade 是基于 Java 21、Spring Boot、Spring Cloud、Spring Cloud Alibaba 和 Maven 的多模块微服务工程。业务域通常由 `blade-service-api/blade-*-api` 契约模块和 `blade-service/*` 或 `blade-ops/*` 实现模块组成，跨服务调用通过 OpenFeign 完成。

遇到文档、配置和现有代码不一致时，按以下优先级判断：

1. 用户当前需求、已确认需求文档和验收标准。
2. 根 `pom.xml`、模块 `pom.xml`、Nacos 配置、SQL 脚本和当前代码。
3. 目标业务域现有 API/Service 契约及实际运行行为。
4. 同类型已完成模块的实现模式。
5. `README.md` 和迁移指南中的背景说明。

发现已确认文档与代码不一致时，不得静默选择对实现最方便的一方。先核对变更历史和关联文档；能够确认实际状态时同步修正文档，无法确认且影响范围或验收时记录为开放问题并向用户说明。

## 2. 工程结构与边界

```text
blade-auth/             # 认证授权服务
blade-gateway/          # Spring Cloud Gateway 统一入口
blade-common/           # 公共常量、启动器和共享能力
blade-service-api/      # Entity、DTO、VO、Feign Client、Fallback 等跨服务契约
blade-service/          # 系统、工作台、日志、示例等业务服务实现
blade-ops/              # 管理、开发、报表、资源、Seata 示例等运维服务
doc/
├── requirements/       # 需求文档
├── design/             # 详细设计
├── database/           # 数据库设计
├── test/               # 测试文档与执行归档
├── templates/          # 文档模板
├── requirements-index.md
├── nacos/              # Nacos 配置
└── sql/                # 全量与升级 SQL
script/                 # Docker、FatJar 和服务脚本
```

- API 模块只定义可共享契约，不放具体业务实现。
- Service 模块实现 Controller、Service、Mapper、Wrapper 和 Feign 服务端接口。
- 跨服务依赖必须通过 `blade-*-api` 和 Feign，禁止 Service 模块直接依赖另一个 Service 模块。
- 数据库结构变化只维护 MySQL，必须同步 `doc/sql/blade` 中的全量与升级脚本，不得只修改临时 SQL。

## 3. 开始开发前

1. 查看 `git status`，保留用户已有改动，不修改无关文件和生成物。
2. 读取 `doc/requirements-index.md` 及目标需求、详细设计和测试文档；没有关联文档时判断任务是否属于需要建档的需求驱动变更。
3. 同时检查目标 Service 模块和对应 API 模块，确认 Entity、DTO、VO、Feign、Controller、Service、Mapper/XML、Wrapper 的现有边界。
4. 检查 Nacos、网关、认证、租户、数据权限、缓存、SQL 和调用方影响，不以单个 Controller 或表面需求推断完整范围。
5. 优先复用项目现有模式和 blade-core 能力；新增依赖、公共抽象或跨模块契约前说明必要性。

## 4. 实现契约

- 新增 Java 文件使用 Apache 2.0 开源许可证头和符合现有风格的类级 Javadoc。
- Entity 基类、Service 和 ServiceImpl 继承关系必须配套：`TenantEntity/BaseEntity` 对应 `BaseService/BaseServiceImpl`，轻量实体按现有模块使用 `IService/ServiceImpl`。
- Long 主键和其他可能超出 JavaScript 安全整数范围的字段，按项目现有序列化约定对外返回。
- Controller 使用项目统一 `R<T>` 响应、参数注解、OpenAPI 注解和权限约定；路由不机械添加 Boot 版专属前缀。
- 自定义 SQL 必须处理逻辑删除、租户和数据权限边界；简单查询优先使用 MyBatis-Plus 现有能力。
- 跨服务接口签名先在 API 模块定义。新增或修改的 Fallback 禁止返回 `null`，应返回 `R.fail(...)` 或明确的类型安全失败结果；调用方不得把降级结果误判为正常业务数据。
- 写操作明确事务边界；跨服务事务、重复请求和并发更新按需求决定 Seata、幂等键、唯一约束、锁或版本校验。
- 日志使用占位符并携带必要业务标识，不记录密码、Token、密钥、连接串和完整敏感请求体。

### 4.1 审计、脱敏等可选横切能力

- 审计、脱敏等横切能力默认不属于基础业务功能。只有需求文档、验收标准或用户当前要求明确提出时，才可纳入设计、数据模型、接口、配置、实现和测试范围。
- 未明确要求时，不得主动增加审计表、审计接口、审计切面、脱敏字段、脱敏规则、权限项、配置项、相关依赖或验收条件，也不得将其作为基础功能完成的前置条件。
- 基础功能应通过清晰的模块边界和稳定契约保持后续扩展能力，但不得为尚未确认的横切能力预先实现抽象层、扩展点、占位字段或空配置。
- 禁止记录密码、Token、密钥、连接串和完整敏感请求体属于通用安全底线，不等同于建设脱敏功能。未要求脱敏时，只需避免采集、持久化和输出不必要的敏感数据，不新增脱敏处理流程。
- 文档中对未要求的审计、脱敏能力应标记为“不涉及”并简述依据，不得写成默认方案、待办事项、开放问题或隐含验收条件。
- 后续新增审计或脱敏要求时，应作为明确的需求变更或独立需求处理，重新评估接口、数据库、权限、配置、发布、回滚和测试影响。

总原则：审计、脱敏等横切能力无明确需求则不设计、不实现、不预留；基础功能仅保持边界开放，并遵守不采集、不记录、不泄露敏感信息的通用安全底线。

## 5. 文档契约

### 5.1 文档体系与模板

- `doc/requirements-index.md` 是需求编号、版本、状态和关联文档的统一登记入口。
- 需求文档使用 `doc/templates/requirements-template.md`。
- 详细设计使用 `doc/templates/detailed-design-template.md`。
- 数据库设计使用 `doc/templates/database-design-template.md`。
- 测试文档使用 `doc/templates/test-document-template.md`。
- 新文档沿用 `REQ-YYYY-NNN` 编号：
  - 需求：`doc/requirements/REQ-YYYY-NNN-short-name.md`
  - 设计：`doc/design/DESIGN-REQ-YYYY-NNN-short-name.md`
  - 数据库：`doc/database/DB-REQ-YYYY-NNN-short-name.md`
  - 测试：`doc/test/TEST-REQ-YYYY-NNN-short-name.md`
- `short-name` 使用简短英文 kebab-case。同一需求的关联文档必须使用相同编号和 `short-name`。
- 模板用于保证信息完整，不得用占位符覆盖已经确认的业务事实；未知信息填写“待指定”“待准备”或“待执行”，不得猜测。

### 5.2 适用范围与建档时机

以下任务默认属于需求驱动任务，应维护完整文档链：

- 新增业务能力、模块、接口、Feign 契约、数据表或重要配置。
- 改变对外行为、权限/租户边界、业务状态、数据语义或兼容策略。
- 跨服务、跨模块、数据库迁移、发布顺序或回滚风险较高的变更。
- 用户明确要求需求、设计、测试或验收文档。

纯拼写修正、注释修正、无行为变化的小型维护可不新建需求编号，但不得借此规避对实际行为变化的记录。无法判断时，先根据改动范围形成文档草稿或向用户确认。

### 5.3 需求驱动流程

需求讨论、设计、开发、测试和验收是同一条文档链，状态变化时统一维护，不得只修改代码或其中一份文档：

1. 需求范围明确后，先在索引分配编号并立即递增“下一个编号”。
2. 从需求模板创建需求文档，明确目标、非目标、范围、流程、业务规则、安全边界和可执行验收标准。
3. 需求达到可评审状态后，从详细设计模板创建设计文档，定义模块边界、API/Feign、事务、配置、测试、发布和回滚方案。
4. 涉及数据库变化时，从数据库设计模板创建独立文档，定义 MySQL 表、字段、约束、索引、历史数据、升级和回滚；不涉及则在需求、设计和索引中写明“不涉及”。
5. 数据库设计确认后，同步 `doc/sql/blade` 中的 MySQL 全量与升级脚本，详细设计只保留数据设计摘要和链接。
6. 实现过程中若实际方案偏离设计，先评估需求和兼容性影响，再同步关联文档、风险和变更记录，禁止仅让代码成为唯一事实来源。
7. 开发完成或详细设计将测试列为待办时，从测试模板创建测试文档，并从验收标准、业务规则和设计风险生成覆盖矩阵与用例。
8. 测试执行后记录逐用例结果、证据、缺陷、环境限制和清理结果，再同步需求、设计、数据库设计和索引。
9. 只有全部必要验收标准和回归项通过，且不存在未关闭的阻断或严重缺陷时，需求才能标记为“已验收”。

### 5.4 版本、状态与关联链接

- 文档版本从 `0.1` 开始；评审确认后使用 `1.0`，确认后的兼容变化递增次版本，重大范围变化递增主版本。
- 每次版本变化必须更新最后更新日期和变更记录。
- 索引状态只使用：待编写、待评审、已确认、设计中、开发中、已验收、已废弃。
- 仅完成代码或部分验证时保持“开发中”，可在正文写“开发完成，待验收”。
- 需求、详细设计、数据库设计和测试文档应互相关联；索引必须登记全部已有文档。
- 文档新增、重命名或关联变化后，检查所有相对链接，避免单向关联、编号不一致或失效路径。

### 5.5 需求文档编写要求

- 需求只描述业务意图、规则、边界和可观察结果，不提前锁死无必要的类名、SQL 或内部实现。
- 每项 Must 需求至少有一个稳定编号和一个 Given/When/Then 风格的验收标准。
- 明确正常、异常、重复、边界和依赖失败流程，以及数据是否改变。
- 涉及登录、角色、租户或数据权限时，写清身份、作用域、资源归属和拒绝行为。
- 明确 API、Feign、数据库、Nacos、部署和调用方是否受影响；不适用项给出简短依据。
- 需求确认后的范围变化必须记录原因、影响和是否需要重新评审。

### 5.6 详细设计编写要求

- 设计必须映射关联需求和验收标准，说明具体影响的 API 与 Service 模块。
- 对 HTTP API 记录路径、参数位置、请求模型、`R<T>` 响应、错误语义、幂等和兼容策略。
- 对 Feign 记录所属 API 模块、服务名、`API_PREFIX`、Java 签名、Fallback、Sentinel/超时和发布顺序。
- 对数据记录 Entity 基类、租户/逻辑删除边界、缓存一致性和数据库设计链接；字段、索引及 MySQL 迁移细节放入数据库设计。
- 对配置记录 Nacos Data ID、环境差异、默认值、敏感性和发布方式；敏感值不得写入文档。
- 对事务记录本地事务、分布式事务、并发控制、重复请求和失败补偿。
- 对发布记录数据库、配置、API 契约、服务实现和调用方的先后顺序及可执行回滚方案。
- 实现完成后记录实际完成范围、设计偏差、已执行验证、未执行项、环境限制和待验收项。

### 5.7 数据库设计编写要求

- 仅设计和维护 MySQL，不要求生成其他数据库方言脚本。
- 必须包含 ER 图、表关系、字段、约束、索引、全量脚本、升级脚本、迁移顺序、回滚和验证。
- 表名、雪花主键、`tenant_id VARCHAR(12)`、框架基础字段、`status`、`is_deleted`、InnoDB 和 utf8mb4 必须符合项目约定。
- ER 实体框、字段清单和 DDL 列顺序应保持一致，并优先呈现业务信息：`id` 固定在首位，其后依次放置核心业务字段、`tenant_id`、框架基础字段。业务关联 ID、编码、名称、内容、版本、状态控制和发布时间等应尽量排在 `tenant_id` 之前，避免租户及技术字段遮挡主要业务结构。
- 租户表必须说明 Entity 基类和 `blade.tenant.tables` 配置；自定义 SQL 必须明确租户与逻辑删除条件。
- 唯一键、并发、历史数据回填和逻辑删除后的业务键复用必须有明确方案，不能仅依赖应用层口头约束。
- 变更必须同步 `blade.mysql.all.create.sql` 和对应 MySQL 升级脚本；禁止只提交 Entity 或临时 SQL。

### 5.8 测试文档编写要求

需求进入“开发完成，待验收”、用户明确要求测试文档，或详细设计将测试列为交付物时，必须在 `doc/test/` 创建或更新关联测试文档，不得另建只有用例列表或结论的简化文件。

1. 保留模板中的文档信息、范围、依据、环境、准备、用例、覆盖矩阵、缺陷、汇总、清理和变更记录。
2. 从需求逐条提取验收标准、业务规则、异常流程、安全约束、权限/租户边界和回归要求，每项验收标准至少对应一个用例。
3. 每个用例至少包含唯一编号、优先级、关联需求、测试层级、目标模块与入口、依赖、前置条件、数据、完整步骤、可观察预期、证据、清理、实际结果和状态。
4. HTTP 用例明确状态码、业务码、响应结构和数据副作用；Feign 用例明确调用、Fallback 和调用方处理；SQL 用例明确目标数据库、升级、重复执行和回滚检查。
5. 安全用例按需覆盖未登录、无权限、越租户、数据权限、敏感字段和日志泄露。
6. 写操作定义唯一数据前缀、允许范围和精确清理步骤。禁止自动删除未知归属、共享租户、生产或历史数据。
7. 用例结果只使用“未执行”“通过”“失败”“阻塞”“不适用”，汇总数量必须与实际用例一致。
8. 只有实际执行并取得证据的检查才能写入结果；编译、静态检查、单元测试、接口联调和业务验收不可互相替代。

### 5.9 测试结果归档

- 测试完成或因明确阻塞阶段性收尾时，记录执行范围、各状态数量、逐用例结果、证据位置、缺陷、数据清理和环境限制。
- 未执行、阻塞、不适用、重试后不稳定和仍需真实环境验证的内容必须保留，不得为了形成通过结论而删除。
- 存在失败、阻塞或必要用例未执行时，需求保持“开发中”或原未完成状态。
- 测试文档、日志摘要和缺陷记录不得包含密码、Token、密钥、连接串或可还原的敏感业务数据。
- 归档后检查实际 diff、文档链接、用例编号、覆盖矩阵和汇总数字，并同步索引及关联文档。

## 6. 命令与验证

当前仓库未提供 Maven Wrapper，使用系统 Maven，并在 JDK 21 环境执行。

```bash
mvn clean compile -DskipTests
mvn clean package -DskipTests
mvn clean package -DskipTests -pl blade-service/blade-system -am
```

- 根据改动范围优先执行模块级编译，再决定是否执行父工程编译或打包。
- 测试由用户执行。AI 不运行 `mvn test`，不启动 Nacos、数据库、Redis 或微服务，也不访问真实接口。
- 只记录实际执行并取得结果的命令；构建成功不等于接口、数据库、权限、租户或业务验收通过。
- 文档变更至少检查 Markdown 结构、编号一致性、相对链接、模板占位符和索引状态，不需要为纯文档变更运行 Maven 构建。

## 7. 变更与提交纪律

- 保持改动聚焦，不覆盖用户未提交的修改，不顺手格式化范围外文件。
- 不手工编辑 `target/`、IDE 配置和其他生成物，除非用户明确要求。
- 修改公共 API、Feign、认证头、Token、租户、数据权限、Nacos 密钥或数据库脚本前，必须核对兼容性和发布影响。
- 不在仓库中写入真实密码、Token、私钥、数据源凭据、本机专用地址或生产数据。
- Git 提交使用项目既有 Gitmoji 加中文描述风格，仅在用户明确要求时提交。

## 8. 完成标准

交付前确认：

- 实现与目标模块现有分层和风格一致，API/Service/Feign 边界正确。
- 认证、权限、租户、逻辑删除、事务、缓存和错误路径按需求处理。
- 数据库和配置变化包含兼容、发布与回滚说明。
- 已运行与风险匹配且获准执行的验证，没有虚构结果。
- 没有无关变更、调试代码、生成物或敏感信息。
- 需求驱动任务已按第 5 节同步需求、详细设计、数据库设计、测试和索引。

最终说明改动文件、行为或文档变化、已执行的验证，以及仍需在真实环境完成的测试和验收。
