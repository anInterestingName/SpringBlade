server:
  port: 80

spring:
  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              # 内置 Locator 会在路由刷新时并发查询全部服务实例，改由只读取服务名的本地实现生成路由
              enabled: false
    loadbalancer:
      retry:
        enabled: true


